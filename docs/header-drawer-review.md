# Route and drawer review · 18 September 2026

The user’s screenshot at user-supplied `Screenshot 2026-09-18 at 22.45.18.png` (not committed) shows the requested surface. Current-run captures below inspect recommendations and itinerary on the same real Cape Town → Kirstenbosch route with two accepted stops. These captures contain different recommendation content and stop counts from the supplied image; findings concern the shared layout rather than exact travel-time text.

1. **Recommendations — working, layout refinement needed.** `artifacts/header-drawer/01-recommendations-before.png` shows readable mock ratings, extra minutes, consistent Apfel typography and one black/orange road route. The duration/filter card occupies the middle of the map; recommendation controls and the footer float independently over map tiles. Move trip information into the header, group cards/actions in a bottom drawer and move itinerary review to a 44-point header control.
2. **Itinerary — working, visually disconnected.** `artifacts/header-drawer/02-itinerary-before.png` shows numbered stops and readable departure/arrival times on the same map. Its large total-duration headline, separate white panel, stacked footer actions and bottom strip of exposed map change the visual structure. Retain the same header, use the shared bottom drawer, show the total in a compact itinerary subtitle and put More stops/Start beside one another.

The revised drawer supports expansion by tap, vertical drag and an accessibility adjustment. Recommendation swipe actions retain button equivalents. Screenshots alone cannot establish VoiceOver, Reduce Motion or complete Dynamic Type compliance; UI checks and manual captures are recorded in the root design QA file. Place/provider data behavior remains unchanged.


Subsequent recommendation request: replace the attraction-heavy route-stop pool with small, interest-matched discoveries. Keep an occasional major attraction without stacking mountain trips. Ratings remain the explicitly requested mock presentation values. Recommendation time now includes the suggested stay plus incremental route travel, with the two components stated on the card and the accepted stay included in the header total. Real venue addresses and descriptions were checked against published venue/City sources. This changes card content intentionally while preserving its typography, photo treatment, corners, buttons and palette.

Final visual checks caught the origin marker under the taller accepted-stop header. Native camera fitting now applies corrections on later frames, with 72 points reserved for the full marker above the projected route. Normal saved-resume and compact/expanded recommendation captures show the corrected state. The combined before/after and focused comparisons, five required design surfaces and final passed result are in `design-qa.md`.
